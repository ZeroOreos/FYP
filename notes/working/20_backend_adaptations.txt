# Backend Adaptations

Purpose
- Keep an exact per-backend record of what was changed locally to make an upstream method run in this repo
- Use this for methods writing, reproducibility notes, and honesty about fidelity

Format
- Model: name
- Status: verified / blocked / scaffolded / partial
- Upstream repo
- Upstream snapshot
- Local wrapper
- Canonical runtime env
- Accelerator status
- Assets used
- Verification date
- Verification input
- Verification output
- Working command
- Local files patched
- Local adaptations
- Fidelity note
- Remaining caveats

Runtime policy
- Move toward one canonical runtime env per model under `Backends/workdirs/<model>/`
- Keep envs isolated by model rather than forcing a single repo-wide PyTorch stack
- Record accelerator support per model in these notes after the env is proven locally
- Add a second model-local env only when a distinct accelerator-oriented stack is genuinely needed
- Near-term runtime goal: try to build a practical MPS-capable venv path that all active models can use, while falling back to model-local envs where compatibility still diverges

Model: simswap
- Status: verified
- Upstream repo: `Backends/sources/attack/SimSwap_upstream` from `neuralchen/SimSwap`
- Upstream snapshot: local preserved repo clone present as of 2026-03-27; commit not yet pinned in this note
- Local wrapper: `Modifiers/attack/simswap/wrapper.py`
- Accelerator status:
- CUDA: upstream-oriented
- CPU: locally patched and verified
- MPS / Apple Metal: one-pair wrapper smoke test now verified under `Backends/workdirs/attack/simswap/venv_mps`
- Assets used:
- `Backends/sources/attack/SimSwap_upstream` upstream inference code
- SimSwap `people` checkpoint assets under `Backends/sources/attack/SimSwap_upstream/checkpoints/people/`
- ArcFace weights under `Backends/sources/attack/SimSwap_upstream/arcface_model/arcface_checkpoint.tar`
- Verification date: 2026-03-27
- Verification input: local one-pair smoke test under `tmp/fomm_verify_assets`
- Verification output:
- `tmp/simswap_wrapper_out/target_demo/source.png`
- records:
- `tmp/simswap_wrapper_records.json`
- Working command:
- `python3 Modifiers/attack/simswap/generate.py ...`
- Local files patched:
- `Modifiers/attack/simswap/wrapper.py`
- `Backends/sources/attack/SimSwap_upstream/test_one_image.py`
- `Backends/sources/attack/SimSwap_upstream/models/fs_model.py`
- Local adaptations:
- added the external-backend wrapper at `Modifiers/attack/simswap/wrapper.py`
- patched `Backends/sources/attack/SimSwap_upstream/test_one_image.py` so the single-image inference path can follow `cuda -> mps -> cpu` instead of assuming CUDA-only execution
- patched `Backends/sources/attack/SimSwap_upstream/models/base_model.py` so model restore/save paths use the resolved runtime device rather than assuming CUDA
- patched `Backends/sources/attack/SimSwap_upstream/models/fs_model.py` so ArcFace checkpoint loading works on newer PyTorch with `weights_only=False` and CPU-aware device handling
- fixed wrapper output-path handling so generated files land in the intended FYP output location
- wrapper now forwards `FYP_TORCH_DEVICE`, enables MPS fallback, and keeps `--python-bin` inside the selected venv when launching the upstream repo
- wrapper redirects `MPLCONFIGDIR` into the attack work area to avoid local cache/temp issues
- built `Backends/workdirs/attack/simswap/venv_mps` with Python 3.11 and verified a one-pair MPS smoke test through `Backends/workdirs/attack/simswap/mps_smoke_out/` and `Backends/workdirs/attack/simswap/mps_smoke_records.json`
- Fidelity note:
- best-effort wrapper around the official upstream repo with CPU-compatibility patches
- Remaining caveats:
- not a byte-faithful original CUDA environment
- current verified path uses `test_one_image.py`, so it does not depend on the separate face-parsing checkpoint used by the whole-image masked scripts

Model: fomm
- Status: verified
- Upstream repo: `Backends/sources/attack/FOMM_upstream` from `AliaksandrSiarohin/first-order-model`
- Upstream snapshot: local preserved repo clone present as of 2026-03-27; commit not yet pinned in this note
- Local wrapper: `Modifiers/attack/fomm/wrapper.py`
- Accelerator status:
- CUDA: upstream-oriented
- CPU: wrapper-verified
- MPS / Apple Metal: one-pair wrapper smoke test now verified under `Backends/workdirs/attack/fomm/venv_mps`
- Assets used:
- `Backends/sources/attack/FOMM_upstream/demo.py`
- clean checkpoint `Backends/assets/attack/fomm/vox-cpk.pth.tar`
- `imageio-ffmpeg`
- Verification date: 2026-03-27
- Verification input: local one-pair smoke test under `tmp/fomm_verify_assets`
- Verification output:
- `tmp/fomm_wrapper_out_default/target_demo/source.png`
- records:
- `tmp/fomm_wrapper_records_default.json`
- Working command:
- `python3 Modifiers/attack/fomm/generate.py ...`
- Local files patched:
- `Modifiers/attack/fomm/wrapper.py`
- `Backends/sources/attack/FOMM_upstream/demo.py`
- `Backends/sources/attack/FOMM_upstream/modules/util.py`
- `Backends/sources/attack/FOMM_upstream/modules/dense_motion.py`
- `Backends/sources/attack/FOMM_upstream/modules/model.py`
- Local adaptations:
- added the external-backend wrapper at `Modifiers/attack/fomm/wrapper.py`
- wrapper now follows `cuda -> mps -> cpu`, forwards `FYP_TORCH_DEVICE`, and resolves `--python-bin` to an absolute path before launching the upstream repo
- default paths were normalized to the upstream config and local checkpoint layout
- `MPLCONFIGDIR` is redirected into the attack work area to avoid temp/cache issues
- wrapper creates a still driving video from the victim image and then extracts the first output frame back into the FYP image-based attack layout
- corrupted early checkpoint downloads were discarded and replaced with a clean checkpoint source
- canonical checkpoint path was cleaned up so `Backends/assets/attack/fomm/vox-cpk.pth.tar` is now the verified working file
- patched the upstream demo path for general device routing so checkpoint restore, model placement, animation tensors, and best-frame alignment can run on MPS
- patched FOMM tensor-creation helpers to stop assuming legacy CUDA tensor type strings, which was the main blocker during the 2026-03-30 MPS smoke test
- MPS smoke-test artifacts now exist under `Backends/workdirs/attack/fomm/mps_smoke_out/` and `Backends/workdirs/attack/fomm/mps_smoke_records.json`
- Fidelity note:
- best-effort wrapper around the official upstream repo; operationally verified
- Remaining caveats:
- reenactment method, not a direct face-swap or morphing method

Model: reface
- Status: verified
- Upstream repo: `Backends/sources/attack/REFace_upstream` from `Sanoojan/REFace`
- Upstream snapshot: local preserved repo clone present as of 2026-03-27; commit not yet pinned in this note
- Local wrapper: `Modifiers/attack/reface/wrapper.py`
- Accelerator status:
- CUDA: upstream-oriented
- CPU: locally patched and verified
- MPS / Apple Metal: one-pair wrapper smoke test now verified under `Backends/workdirs/attack/reface/venv_mps`
- Assets used:
- official REFace repo
- `Backends/assets/attack/reface/last.ckpt`
- persistent support assets under `Backends/assets/attack/reface/support/`
- upstream `Other_dependencies/*` bundle including ArcFace, face parsing, and dlib landmark files
- local Hugging Face model cache for `openai/clip-vit-large-patch14` under `Backends/assets/attack/reface/support/hf_cache/`
- local Torch asset cache under `Backends/assets/attack/reface/support/torch_home/` for `resnet18-5c106cde.pth`, LPIPS AlexNet, and HardNet assets
- `dlib-bin==19.24.6`
- Verification date: 2026-03-27
- Verification input:
- official example pair
- source: `Backends/sources/attack/REFace_upstream/examples/FaceSwap/Source/will_smith.jpeg`
- target: `Backends/sources/attack/REFace_upstream/examples/FaceSwap/Target/elon.jpeg`
- Verification output:
- `tmp/reface_verify_out/elon_demo/will_smith.jpeg`
- upstream sample output:
- `Backends/workdirs/attack/reface/00000/run/results/0/000000000000.png`
- MPS smoke-test output:
- `Backends/workdirs/attack/reface/mps_smoke_out/victim0/052628.jpg`
- MPS smoke-test records:
- `Backends/workdirs/attack/reface/mps_smoke_records.json`
- Working command:
- `python3 Modifiers/attack/reface/generate.py Backends/sources/attack/REFace_upstream/examples/FaceSwap tmp/reface_verify_assets/pairs.npz tmp/reface_verify_out tmp/reface_verify_records.json --overwrite --ddim-steps 1 --precision full`
- Local files patched:
- `Modifiers/attack/reface/wrapper.py`
- `Backends/sources/attack/REFace_upstream/ldm/modules/encoders/modules.py`
- `Backends/sources/attack/REFace_upstream/scripts/inference_swap_selected.py`
- `Backends/sources/attack/REFace_upstream/ldm/models/diffusion/ddim.py`
- `Backends/sources/attack/REFace_upstream/ldm/models/diffusion/plms.py`
- `Backends/sources/attack/REFace_upstream/ldm/models/diffusion/ddpm.py`
- `Backends/sources/attack/REFace_upstream/ldm/data/video_swap_dataset.py`
- `Backends/sources/attack/REFace_upstream/pretrained/face_parsing/face_parsing_demo.py`
- `Backends/sources/attack/REFace_upstream/pretrained/face_parsing/model.py`
- `Backends/sources/attack/REFace_upstream/main.py`
- Local adaptations:
- added a new FYP wrapper at `Modifiers/attack/reface/`
- registered `reface` in `Utility/runtime.py`
- wrapper now resolves `--python-bin` to an absolute path before launching the upstream repo
- wrapper now sets local cache env vars for Matplotlib, Hugging Face, and Torch so upstream downloads resolve in stable repo-owned locations instead of `tmp/`
- wrapper exports `REFACE_CLIP_MODEL_DIR` and offline HF flags so CLIP loads from a local cache instead of calling Hugging Face at runtime
- wrapper now also exports `FYP_TORCH_DEVICE` and enables MPS fallback for the upstream script
- fixed wrapper output resolution so it no longer falls back to copied source images when upstream produces no result
- patched `ldm/modules/encoders/modules.py` so CLIP tokenizer/model loading can resolve from the local CLIP model directory
- removed the stale `clip` package import from `ldm/modules/encoders/modules.py` because this repo now uses the local Hugging Face CLIP path instead
- patched `scripts/inference_swap_selected.py` for CPU/MPS-safe execution:
- model load avoids unconditional `.cuda()`
- reference image tensor uses `float32` on non-CUDA devices
- autocast only activates on CUDA
- DataLoader uses `num_workers=0` and `pin_memory=False` on non-CUDA devices
- device resolution now follows `cuda -> mps -> cpu`
- patched `ldm/models/diffusion/ddim.py` and `ldm/models/diffusion/plms.py` to register buffers on the actual model device instead of assuming CUDA
- patched `ldm/models/diffusion/ddpm.py` so ArcFace-related checkpoint loads use `map_location=torch.device('cpu')` when CUDA is unavailable
- patched Lightning import compatibility in `main.py` and `ldm/models/diffusion/ddpm.py` so the current venv can use `pytorch-lightning>=2`
- made `wandb` optional in `ldm/models/diffusion/ddpm.py` so inference does not depend on training-time logging packages
- removed dead `clip` and `bezier` imports from `ldm/data/video_swap_dataset.py`
- removed the stray `turtle` import from `ldm/data/video_swap_dataset.py` because it pulled in unavailable `tkinter` support and broke the preserved verification path
- patched face parsing code in `pretrained/face_parsing/face_parsing_demo.py` and `pretrained/face_parsing/model.py`:
- CPU-aware checkpoint deserialization
- CPU-safe mean/std and bicubic downsampling path
- parser defaults to CPU when CUDA is unavailable
- seeded all required local caches and weights that upstream tried to download at runtime:
- OpenAI CLIP `openai/clip-vit-large-patch14`
- PyTorch `resnet18-5c106cde.pth`
- LPIPS AlexNet weights
- HardNet checkpoint
- installed `dlib-bin` instead of source-building `dlib`, because source build failed on this machine’s CMake path
- Fidelity note:
- best-effort wrapper around the official REFace repo; verified working on CPU and on a one-pair MPS smoke test with offline cache seeding and targeted compatibility patches
- Remaining caveats:
- not a byte-faithful original CUDA environment
- verification used `--ddim-steps 1` for a practical smoke test, not a full-quality benchmark setting

Model: advfacegan
- Status: verified
- Upstream repo: intended preserved backend is `Backends/sources/attack/AdvFaceGAN_upstream`
- Upstream snapshot: local preserved repo clone present as of 2026-03-27; commit not yet pinned in this note
- Local wrapper: `Modifiers/attack/advfacegan/wrapper.py`
- Accelerator status:
- CUDA: upstream-oriented
- CPU: wrapper-verified
- MPS / Apple Metal: one-pair wrapper smoke test now verified under `Backends/workdirs/attack/advfacegan/venv_mps`
- Assets used:
- preserved upstream generator checkpoint `Backends/sources/attack/AdvFaceGAN_upstream/defense/00940_generator.pth`
- upstream sample images `Backends/sources/attack/AdvFaceGAN_upstream/defense/clean.png` and `Backends/sources/attack/AdvFaceGAN_upstream/defense/fake.png`
- local FaceNet embedder path via `facenet_pytorch`
- Verification date: 2026-03-29
- Verification input: local one-pair smoke test under `tmp/advfacegan_verify_assets`
- Verification output:
- `tmp/advfacegan_verify_out/fake_demo/clean.png`
- records:
- `tmp/advfacegan_verify_records_checkpoint.json`
- MPS smoke-test output:
- `Backends/workdirs/attack/advfacegan/mps_smoke_out/fake_demo/clean.png`
- MPS smoke-test records:
- `Backends/workdirs/attack/advfacegan/mps_smoke_records.json`
- Working command:
- `python3 Modifiers/attack/advfacegan/generate.py Backends/sources/attack/AdvFaceGAN_upstream/defense tmp/advfacegan_verify_assets/pairs.npz tmp/advfacegan_verify_out tmp/advfacegan_verify_records_checkpoint.json --generator-checkpoint Backends/sources/attack/AdvFaceGAN_upstream/defense/00940_generator.pth --overwrite`
- Local files patched:
- `Modifiers/attack/advfacegan/wrapper.py`
- Local adaptations:
- wrapper keeps the local optimization fallback for checkpoint-free runs
- checkpoint-backed inference now dynamically loads the preserved upstream `Generator` class instead of forcing weights into the local recreation architecture
- wrapper detects whether the checkpoint expects 3-channel source-only or 6-channel source-plus-target input from `conv1.conv.weight`
- CPU smoke test was verified against the preserved upstream checkpoint and sample images
- MPS smoke test was also verified against that same preserved upstream checkpoint through `Backends/workdirs/attack/advfacegan/venv_mps`
- `Backends/assets/attack/advfacegan/ckpts.zip` is not currently a usable verified source for this wrapper path
- `Backends/assets/attack/advfacegan/ckpts.zip` currently appears corrupt or incomplete during local extraction attempts, so it should not be treated as a ready MPS verification source
- the current working checkpoint path is the preserved upstream file `Backends/sources/attack/AdvFaceGAN_upstream/defense/00940_generator.pth`, so the broken asset zip is not needed for verification
- Fidelity note:
- best-effort wrapper around the preserved upstream repository with checkpoint-adapter behavior for local verification; CPU and one-pair MPS smoke tests now succeed with the preserved upstream checkpoint
- Remaining caveats:
- not a byte-faithful original training or CUDA environment
- verified checkpoint path currently uses the source-only upstream defense generator, so target-conditioning behavior still depends on which checkpoint is supplied

Model: mordiff
- Status: verified
- Upstream repo: intended preserved backend is `Backends/sources/attack/MorDIFF_upstream`
- Upstream snapshot: local preserved repo clone present as of 2026-03-27; commit not yet pinned in this note
- Local wrapper: `Modifiers/attack/mordiff/wrapper.py`
- Canonical runtime env:
- CPU: `Backends/workdirs/attack/mordiff/venv_cpu`
- MPS: `Backends/workdirs/attack/mordiff/venv_mps`
- Accelerator status:
- CPU: verified via dedicated backend-local env
- MPS / Apple Metal: verified via dedicated backend-local env
- Assets used:
- upstream sample aligned images:
- `Backends/sources/attack/MorDIFF_upstream/test/aligned/001_03.png`
- `Backends/sources/attack/MorDIFF_upstream/test/aligned/019_03.png`
- imported support dependency:
- `Backends/sources/dependencies/diffae` at commit `00c57d3f626f28bf9ed8aff58d90baab25de3af4`
- imported official DiffAE checkpoint assets:
- `Backends/sources/dependencies/diffae/checkpoints/ffhq256_autoenc/last.ckpt`
- `Backends/sources/dependencies/diffae/checkpoints/ffhq256_autoenc/latent.pkl`
- Verification date: 2026-03-29
- Verification input: local one-pair smoke test under `tmp/mordiff_verify_assets`
- Verification output:
- fallback smoke-test output:
- `tmp/mordiff_verify_out/019_03_demo/001_03.png`
- checkpoint-backed native smoke-test output:
- `tmp/mordiff_verify_out_native/019_03_demo/001_03.png`
- env-backed checkpoint smoke-test output:
- `tmp/mordiff_env_verify_out/019_03_demo/001_03.png`
- MPS env-backed native smoke-test output:
- `tmp/mordiff_mps_venv_out/morph_001_03_and_019_03.png`
- MPS wrapper smoke-test output:
- `tmp/mordiff_wrapper_mps_out/019_03_demo/001_03.png`
- latest native-attempt records:
- `tmp/mordiff_verify_records_native.json`
- env-backed records:
- `tmp/mordiff_env_verify_records.json`
- MPS wrapper records:
- `tmp/mordiff_wrapper_mps_records.json`
- records:
- `tmp/mordiff_verify_records.json`
- Working command:
- smoke-test command:
- `python3 Modifiers/attack/mordiff/generate.py Backends/sources/attack/MorDIFF_upstream/test/aligned tmp/mordiff_verify_assets/pairs.npz tmp/mordiff_verify_out tmp/mordiff_verify_records.json --overwrite`
- checkpoint-backed reduced native smoke-test command:
- `python3 Modifiers/attack/mordiff/generate.py Backends/sources/attack/MorDIFF_upstream/test/aligned tmp/mordiff_verify_assets/pairs.npz tmp/mordiff_verify_out_native tmp/mordiff_verify_records_native.json --overwrite --extra-arg=--stochastic-steps --extra-arg=20 --extra-arg=--render-steps --extra-arg=2 --extra-arg=--save-midpoint-only`
- env-backed reduced native smoke-test command:
- `Backends/workdirs/attack/mordiff/venv_cpu/bin/python Modifiers/attack/mordiff/generate.py Backends/sources/attack/MorDIFF_upstream/test/aligned tmp/mordiff_verify_assets/pairs.npz tmp/mordiff_env_verify_out tmp/mordiff_env_verify_records.json --overwrite --extra-arg=--stochastic-steps --extra-arg=--render-steps --extra-arg=2 --extra-arg=--save-midpoint-only`
- MPS native smoke-test command:
- `MPLCONFIGDIR=tmp/mpl_mordiff_venv_mps Backends/workdirs/attack/mordiff/venv_mps/bin/python Backends/sources/attack/MorDIFF_upstream/morph_two_images.py --img1 Backends/sources/attack/MorDIFF_upstream/test/aligned/001_03.png --img2 Backends/sources/attack/MorDIFF_upstream/test/aligned/019_03.png --output tmp/mordiff_mps_venv_out --device mps --stochastic-steps 5 --render-steps 2 --save-midpoint-only`
- MPS wrapper smoke-test command:
- `MPLCONFIGDIR=tmp/mpl_mordiff_wrapper_mps Backends/workdirs/attack/mordiff/venv_mps/bin/python Modifiers/attack/mordiff/generate.py Backends/sources/attack/MorDIFF_upstream/test/aligned tmp/mordiff_verify_assets/pairs.npz tmp/mordiff_wrapper_mps_out tmp/mordiff_wrapper_mps_records.json --overwrite --python-bin Backends/workdirs/attack/mordiff/venv_mps/bin/python --extra-arg=--device --extra-arg=mps --extra-arg=--stochastic-steps --extra-arg=5 --extra-arg=--render-steps --extra-arg=2 --extra-arg=--save-midpoint-only`
- Local files patched:
- `Modifiers/attack/mordiff/wrapper.py`
- `Backends/sources/attack/MorDIFF_upstream/morph_two_images.py`
- `Backends/sources/dependencies/diffae/experiment.py`
- `Backends/sources/dependencies/diffae/dataset.py`
- `Backends/sources/dependencies/diffae/lmdb_writer.py`
- `Backends/sources/dependencies/diffae/metrics.py`
- Local adaptations:
- wrapper scaffold aligned to the shared external-backend pattern
- wrapper invocation itself is wired correctly against the preserved upstream `morph_two_images.py` entry script
- patched `Backends/sources/attack/MorDIFF_upstream/morph_two_images.py` so it resolves the cloned local DiffAE dependency under `Backends/sources/dependencies/diffae` instead of hardcoding `./diffae-master`
- patched the same script for CPU-safe device selection and PyTorch 2.6-safe checkpoint loading
- added explicit smoke-test flags to the same script so checkpoint-backed runs can reduce stochastic steps, reduce render steps, and save only the midpoint morph image when a practical CPU verification is needed
- patched `Backends/sources/dependencies/diffae/experiment.py` for NumPy 2.x compatibility by replacing the removed `numpy.lib.function_base.flip` import
- patched `Backends/sources/dependencies/diffae/dataset.py` and `Backends/sources/dependencies/diffae/lmdb_writer.py` so missing `lmdb` only fails when LMDB-backed dataset paths are actually used
- patched `Backends/sources/dependencies/diffae/metrics.py` so optional evaluation-only dependencies such as `pytorch_fid` and `lpips` are imported lazily instead of blocking inference-only use
- patched the same MorDIFF script so midpoint-only smoke-test mode no longer crashes after saving the native morph image
- tightened the wrapper so fallback blending only triggers for recognizable missing-dependency or missing-asset states instead of any generic non-zero subprocess exit
- fixed wrapper output resolution so it ignores the `result/` directory itself and copies the actual generated morph image back into the FYP output layout
- built a dedicated backend-local virtualenv at `Backends/workdirs/attack/mordiff/venv_cpu` using the known-working CPU package stack
- built a second dedicated backend-local virtualenv at `Backends/workdirs/attack/mordiff/venv_mps` using Homebrew Python 3.11 plus a MorDIFF-compatible MPS package stack
- wrapper now prefers that dedicated MorDIFF interpreter automatically when it exists, while still allowing `--python-bin` override
- confirmed the wrapper path itself now succeeds on MPS through `Backends/workdirs/attack/mordiff/venv_mps`
- when the upstream script fails specifically because the local DiffAE dependency tree is missing, the wrapper can still emit a deterministic 50/50 blend image as a last-resort smoke-test fallback and record that explicitly
- Fidelity note:
- best-effort external wrapper around the native MorDIFF script with the official DiffAE checkpoint and local compatibility patches; checkpoint-backed CPU and reduced-step MPS smoke tests now complete successfully
- Remaining caveats:
- imported DiffAE support code removes the old `templates` import-path blocker and the official checkpoint is now present locally
- current verified native smoke test uses reduced steps and midpoint-only output to stay practical on CPU; this is suitable for verification, not for claiming full-quality benchmark settings
- current verified MPS path also uses reduced steps and midpoint-only output; use it as a native acceleration proof path, not as a full benchmark-quality setting
- full default-step CPU runs may still be impractically slow or unstable on this machine

Model: dim
- Status: partial
- Upstream repo: native import is `Backends/sources/attack/DiM_native_upstream` from `zblasingame/Greedy-DiM`
- Upstream snapshot: imported 2026-03-29 at commit `37adc6047a17d844b64af27fce9018d034c93c18`
- Local wrapper: `Modifiers/attack/dim/wrapper.py`
- Accelerator status:
- CPU: wrapper-verified
- CUDA / CoreML path: not yet specialized
- Assets used:
- local approximation wrapper only
- native upstream import:
- `Backends/sources/attack/DiM_native_upstream`
- native runner state:
- `Backends/sources/attack/DiM_native_upstream/run_dim.py.gpg`
- release gate form:
- `Backends/sources/attack/DiM_native_upstream/CITeR_SoftwareReleaseAgreeement.docx`
- verification sample images:
- `Backends/sources/attack/MorDIFF_upstream/test/aligned/001_03.png`
- `Backends/sources/attack/MorDIFF_upstream/test/aligned/019_03.png`
- Verification date: 2026-03-29
- Verification input: local one-pair smoke test under `tmp/dim_verify_assets`
- Verification output:
- `tmp/dim_verify_out/019_03_demo/001_03.png`
- records:
- `tmp/dim_verify_records.json`
- Working command:
- `python3 Modifiers/attack/dim/generate.py Backends/sources/attack/MorDIFF_upstream/test/aligned tmp/dim_verify_assets/pairs.npz tmp/dim_verify_out tmp/dim_verify_records.json --overwrite`
- Local files patched:
- `Modifiers/attack/dim/wrapper.py`
- `Modifiers/attack/dim/generate.py`
- `Modifiers/attack/dim/README.md`
- `Utility/runtime.py`
- `Backends/manifest.json`
- `Backends/README.md`
- Local adaptations:
- added a first-class local DiM engineered recreation backend slot for pipeline integration
- wrapper uses a face-centered soft mask, color-statistics alignment, and symmetric blending to create a deterministic morph-like probe
- native Greedy-DiM / DiM source was imported, but its runnable morphing script remains encrypted and requires the upstream passphrase-release process
- local smoke test was re-confirmed after native-source import using `tmp/dim_verify_out/019_03_demo/001_03.png` and `tmp/dim_verify_records.json`
- current repo status should be described as an engineered recreation that runs locally, not as a faithful reproduction of the native DiM release
- further DiM development is not planned under the current roadmap
- Fidelity note:
- engineered recreation / best attempt for smoke testing and pipeline continuity
- Remaining caveats:
- exact native repo snapshot is now imported, but runnable code access is still gated by the upstream release process
- the CITeR software release agreement text does not appear to require a US-affiliated institution in general; it says software may be available to researchers outside CITeR after a signed release agreement, and requires the requestor supervisor signature
- the US Government language in the release form is a special indemnification carve-out checkbox, not the general eligibility rule
- the imported native implementation also expects DiffAE and ArcFace assets before it can be executed faithfully
- this path is suitable for local smoke tests, not yet for paper claims about faithful DiM reproduction

Morph roadmap after DiM
- keep `dim` in the repo as a documented engineered recreation that runs, but do not plan further development work on it for now
- today's next morphing build target is `mipgan`
- if a future diffusion-morph replacement is needed beyond that, inspect `LADIMO` and then `StableMorph`, with the same provenance and openness checks as before

Model: mipgan
- Status: partial
- Upstream repo: intended preserved backend is `Backends/sources/attack/MIPGAN_upstream`
- Upstream snapshot: official public portal snapshot imported 2026-03-30 from `HaoyuZhang/face-morphing-attack-ntnu-MIPGAN-public` at commit `ba42a941b7ce21e5a4c52eb54fcf04cecc3f4378`
- Local wrapper: `Modifiers/attack/mipgan/wrapper.py`
- Accelerator status:
- not yet vetted
- Assets used:
- official public portal files:
- `Backends/sources/attack/MIPGAN_upstream/README.md`
- `Backends/sources/attack/MIPGAN_upstream/MIPGAN-license-240916.pdf`
- local fallback wrapper path only
- verification sample images:
- `Backends/sources/attack/MorDIFF_upstream/test/aligned/001_03.png`
- `Backends/sources/attack/MorDIFF_upstream/test/aligned/019_03.png`
- Verification date: 2026-03-29
- Verification input: local one-pair smoke test under `tmp/mipgan_verify_assets`
- Verification output:
- fallback smoke-test output:
- `tmp/mipgan_verify_out/019_03_demo/001_03.png`
- records:
- `tmp/mipgan_verify_records.json`
- Working command:
- smoke-test command:
- `python3 Modifiers/attack/mipgan/generate.py Backends/sources/attack/MorDIFF_upstream/test/aligned tmp/mipgan_verify_assets/pairs.npz tmp/mipgan_verify_out tmp/mipgan_verify_records.json --overwrite`
- Local files patched:
- `Modifiers/attack/mipgan/wrapper.py`
- Local adaptations:
- wrapper scaffold aligned to the shared external-backend pattern
- when no local upstream entry script is present, the wrapper now emits a deterministic 50/50 blend image as a smoke-test fallback and records that explicitly
- imported the official public MIPGAN portal snapshot into `Backends/sources/attack/MIPGAN_upstream` without preserving nested Git metadata
- current immediate goal is to replace as much of the fallback path as possible with a more real buildable MIPGAN path today
- Fidelity note:
- best-attempt external wrapper; the official public portal snapshot is now present in the repo, but the runnable native MIPGAN package is still not present locally
- Remaining caveats:
- official provenance is now established for the public portal snapshot, but the runnable entry script and pretrained models remain in the separate password-protected distribution package referenced by the portal README

Recognition model: adaface
- Status: bootstrapped
- Upstream repo: preserved backend is `Backends/sources/AdaFace_upstream` from `mk-minchul/AdaFace`
- Upstream snapshot: imported 2026-03-30 at commit `c60eaa786a42c03444f3df7096dbaf9d57ae010d`
- Local wrapper: `Models/AdaFace/generate.py`
- Accelerator status:
- `cuda -> mps -> cpu` via shared `Utility/runtime.py`
- MPS: verified outside sandbox
- Assets used:
- source snapshot: `Backends/sources/AdaFace_upstream`
- asset root: `Backends/assets/recognition/adaface`
- workdir root: `Backends/workdirs/recognition/adaface`
- expected checkpoint path: `Backends/assets/recognition/adaface/pretrained/adaface_ir50_ms1mv2.ckpt`
- Verification date: 2026-03-30
- Verification input: `tmp/recognition_smoke/CelebA/motion_len9_smoke`
- Verification output:
- `tmp/recognition_smoke_out/adaface_embeddings.npz`
- Working command:
- `MPLCONFIGDIR=Backends/workdirs/recognition/adaface/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=1 Backends/workdirs/recognition/adaface/venv/bin/python Models/AdaFace/generate.py tmp/recognition_smoke/CelebA/motion_len9_smoke tmp/recognition_smoke_out/adaface_embeddings.npz 2 0 --device auto --allow-random-init`
- Local files patched:
- `Models/AdaFace/generate.py`
- `Utility/runtime.py`
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `README.md`
- Local adaptations:
- imported the native AdaFace source into the repo-owned external backend area and removed nested Git metadata after vendoring
- added a repo-local AdaFace generator that loads the native backbone from the vendored upstream tree
- runtime env now keeps AdaFace torch cache, matplotlib cache, assets, and venv under repo-owned recognition paths
- smoke test was run with `--device auto` outside sandbox and resolved to `mps` on this machine
- smoke test intentionally used random initialization because no pretrained AdaFace checkpoint is present locally yet
- Fidelity note:
- native-backbone external integration is wired and smoke-tested; benchmark-quality use still requires a real upstream checkpoint in the repo-owned asset path

Recognition model: magface
- Status: bootstrapped
- Upstream repo: preserved backend is `Backends/sources/MagFace_upstream` from `IrvingMeng/MagFace`
- Upstream snapshot: imported 2026-03-30 at commit `99bae614ac2643b9694bf18e0c5645272ff6acfa`
- Local wrapper: `Models/MagFace/generate.py`
- Accelerator status:
- `cuda -> mps -> cpu` via shared `Utility/runtime.py`
- MPS: verified outside sandbox
- Assets used:
- source snapshot: `Backends/sources/MagFace_upstream`
- asset root: `Backends/assets/recognition/magface`
- workdir root: `Backends/workdirs/recognition/magface`
- expected checkpoint path: `Backends/assets/recognition/magface/pretrained/magface_iresnet100_ms1mv2.pth`
- Verification date: 2026-03-30
- Verification input: `tmp/recognition_smoke/CelebA/motion_len9_smoke`
- Verification output:
- `tmp/recognition_smoke_out/magface_embeddings.npz`
- Working command:
- `MPLCONFIGDIR=Backends/workdirs/recognition/magface/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=1 Backends/workdirs/recognition/magface/venv/bin/python Models/MagFace/generate.py tmp/recognition_smoke/CelebA/motion_len9_smoke tmp/recognition_smoke_out/magface_embeddings.npz 2 0 --device auto --allow-random-init`
- Local files patched:
- `Models/MagFace/generate.py`
- `Utility/runtime.py`
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `README.md`
- Local adaptations:
- imported the native MagFace source into the repo-owned external backend area and removed nested Git metadata after vendoring
- added a repo-local MagFace generator that loads the native iResNet backbone from the vendored upstream tree
- runtime env now keeps MagFace torch cache, matplotlib cache, assets, and venv under repo-owned recognition paths
- smoke test was run with `--device auto` outside sandbox and resolved to `mps` on this machine
- smoke test intentionally used random initialization because no pretrained MagFace checkpoint is present locally yet
- Fidelity note:
- native-backbone external integration is wired and smoke-tested; benchmark-quality use still requires a real upstream checkpoint in the repo-owned asset path

Protection backend: fawkes
- Status: verified
- Upstream repo: preserved backend is `Backends/sources/protection/Fawkes_upstream` from `Shawn-Shan/fawkes`
- Upstream snapshot: imported 2026-03-30 at commit `aedaa82d22504c4e7dbc86de869a6e0d943a5f2a`
- Defense layer: proactive privacy / cloaking
- Assets used:
- source snapshot: `Backends/sources/protection/Fawkes_upstream`
- asset root: `Backends/assets/protection/fawkes`
- workdir root: `Backends/workdirs/protection/fawkes`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/fawkes/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- `Backends/sources/protection/Fawkes_upstream/fawkes/differentiator.py`
- Local adaptations:
- imported the official upstream source into the protection subtree and removed nested Git metadata after vendoring
- added a repo-local wrapper at `Models/protection/fawkes/wrapper.py`
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- rebuilt the local env with Homebrew Python 3.11 because the earlier Python 3.14 path could not support a practical TensorFlow/Keras compatibility route here
- updated `scripts/bootstrap_protection_envs.py` so `fawkes` now targets the local Python 3.11 interpreter when available
- installed a modernized local CPU stack around `tensorflow-macos==2.15.0`, `keras==2.15.0`, and a back-pinned `mtcnn==0.1.1`
- pinned `setuptools<81` because the vendored upstream code still imports `pkg_resources`
- fixed the wrapper so `--python-bin` keeps the selected venv interpreter instead of resolving out to the base Homebrew Python
- let the official upstream runtime download its own released extractor checkpoint into the vendored package path
- patched `fawkes/differentiator.py` to prefer `tf.keras.optimizers.legacy.Adadelta`, which matches the TensorFlow warning-guided compatibility path on this machine
- verified a one-image local smoke run on CPU through the repo wrapper
- verified command:
- `Backends/workdirs/protection/fawkes/venv/bin/python Models/protection/fawkes/wrapper.py tmp/fawkes_smoke_in tmp/fawkes_smoke_out --python-bin Backends/workdirs/protection/fawkes/venv/bin/python --device cpu --batch-size 1 --mode low --no-align`
- verified artifacts:
- `tmp/fawkes_smoke_out/0.jpg`
- `tmp/fawkes_smoke_out/_fawkes_summary.json`
- Fidelity note:
- wrapper now completes a whole local smoke pipeline, but this should still be described as a modern compatibility port rather than an untouched original `tensorflow==2.4.1` environment
- the verified path uses Python 3.11 plus TensorFlow/Keras 2.15 compatibility fixes, so it proves practical local runtime viability more than pristine historical environment reproduction

Protection backend: faceshield
- Status: blocked
- Upstream repo: preserved backend is `Backends/sources/protection/FaceShield_upstream` from `kuai-lab/iccv25_faceshield`
- Upstream snapshot: imported 2026-03-30 at commit `82ea66949e79a34b1edd943fb7b5c9528be690f5`
- Defense layer: proactive image protection
- Assets used:
- source snapshot: `Backends/sources/protection/FaceShield_upstream`
- asset root: `Backends/assets/protection/faceshield`
- workdir root: `Backends/workdirs/protection/faceshield`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/faceshield/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- Local adaptations:
- imported the official upstream source into the protection subtree and removed nested Git metadata after vendoring
- added a repo-local wrapper at `Models/protection/faceshield/wrapper.py`
- wrapper keeps the backend in the protection subtree and surfaces its CUDA-only upstream limitation explicitly
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- verified a minimal local smoke attempt on CPU through the wrapper and it fails immediately by design when the resolved runtime is non-CUDA
- upstream `attack.py` also hardcodes CUDA-oriented execution details such as `torch.amp.autocast('cuda')` and `torch.cuda.empty_cache()` inside the main attack loop
- Fidelity note:
- keep this backend blocked under the fidelity-first policy on this Apple Silicon machine
- a fake MPS or CPU path would be a semantic port rather than a faithful verification of the released upstream method
- Queue note:
- preserve this backend in the repo for provenance and future CUDA-capable verification, but remove it from the active local defense-evaluation queue

Protection backend: falco
- Status: verified
- Upstream repo: preserved backend is `Backends/sources/protection/FALCO_upstream` from `chi0tzp/FALCO`
- Upstream snapshot: imported 2026-03-30 at commit `1cbf5f90ffc8de1ac1bafb287a2c1a9882bf798a`
- Defense layer: proactive anonymization
- Assets used:
- source snapshot: `Backends/sources/protection/FALCO_upstream`
- asset root: `Backends/assets/protection/falco`
- workdir root: `Backends/workdirs/protection/falco`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/falco/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- `Backends/sources/protection/FALCO_upstream/extract_features.py`
- `Backends/sources/protection/FALCO_upstream/lib/arcface.py`
- `Backends/sources/protection/FALCO_upstream/create_fake_dataset.py`
- `Backends/sources/protection/FALCO_upstream/pair_nn.py`
- `Backends/sources/protection/FALCO_upstream/invert.py`
- `Backends/sources/protection/FALCO_upstream/anonymize.py`
- `Backends/sources/protection/FALCO_upstream/lib/id_loss.py`
- `Backends/sources/protection/FALCO_upstream/lib/attr_loss.py`
- `Backends/sources/protection/FALCO_upstream/models/stylegan2/op/fused_act.py`
- `Backends/sources/protection/FALCO_upstream/models/stylegan2/op/upfirdn2d.py`
- Local adaptations:
- imported the official upstream source into the protection subtree and removed nested Git metadata after vendoring
- added a repo-local stage wrapper at `Models/protection/falco/wrapper.py`
- wrapper preserves FALCO's original multi-stage workflow instead of pretending it is a one-command folder transform
- rebuilt the local env with Homebrew Python 3.11 because this torch-heavy stack should not stay on the earlier Python 3.14 path
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- wired the wrapper plus `extract_features.py` and `create_fake_dataset.py` to honor `mps` as a real runtime device instead of collapsing everything non-CUDA to CPU
- patched the ArcFace checkpoint load to use CPU-first deserialization so the preserved CUDA-saved weights can load on this machine
- patched the ArcFace forward path so the MPS smoke route uses on-device interpolation instead of the MPS-broken non-divisible adaptive pool
- patched `pair_nn.py` so NN indices are read correctly from the scikit-learn result shape
- patched `invert.py` with a smoke-mode `--skip-align` path because the original face-aligner stack is still CUDA-era code
- patched `IDLoss` and `AttrLoss` for portable checkpoint loading and MPS-safe resize behavior
- patched the vendored StyleGAN2 op loaders so non-CUDA runs use native PyTorch fallback code instead of hard-failing on CUDA extension builds
- staged a tiny local CelebA-HQ-style fixture under `tmp/falco_smoke_celebahq`
- symlinked `Backends/sources/protection/FALCO_upstream/models/pretrained` to the preserved asset tree under `Backends/assets/protection/falco/models/pretrained`
- verified the `extract_features.py` stage on native MPS outside sandbox with fallback disabled using ArcFace only
- verified the `create_fake_dataset.py` stage on native MPS outside sandbox using `stylegan2_ffhq512`, one sample, and ArcFace only
- verified the `pair_nn.py` stage on the tiny local feature sets using ArcFace only
- verified the `invert.py` smoke path on native MPS outside sandbox using `--skip-align`
- verified the `anonymize.py` smoke path on native MPS outside sandbox using one epoch
- a reduced FALCO smoke pipeline is now native MPS end to end
- verified command:
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 /Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin/python -u /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/extract_features.py --dataset celebahq --dataset-root /Users/jeromeharianto/Documents/School2/FYP/tmp/falco_smoke_celebahq --batch-size 2 --no-clip --no-farl --no-dino --verbose`
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 FYP_TORCH_DEVICE=mps /Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin/python -u /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/create_fake_dataset.py --gan stylegan2_ffhq512 --truncation 0.7 --num-samples 1 --no-clip --no-farl --no-dino --verbose`
- `PYTHONUNBUFFERED=1 /Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin/python -u /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/pair_nn.py --real-dataset celebahq --fake-dataset-root /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/datasets/fake/fake_dataset_stylegan2_ffhq512-0.7-1-ArcFace --algorithm auto --metric euclidean --verbose`
- `PATH=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin:$PATH PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 FYP_TORCH_DEVICE=mps /Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin/python -u /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/invert.py --dataset celebahq --dataset-root /Users/jeromeharianto/Documents/School2/FYP/tmp/falco_smoke_celebahq --batch-size 2 --skip-align --verbose`
- `PATH=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin:$PATH PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 FYP_TORCH_DEVICE=mps /Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/falco/venv/bin/python -u /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/anonymize.py --dataset celebahq --dataset-root /Users/jeromeharianto/Documents/School2/FYP/tmp/falco_smoke_celebahq --subset train+val+test --fake-nn-map /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/FALCO_upstream/datasets/fake/fake_dataset_stylegan2_ffhq512-0.7-1-ArcFace/arcface_auto_euclidean_nn_map_celebahq.json --epochs 1 --verbose`
- extract-features ArcFace-only smoke path is native MPS end to end
- verified artifacts:
- `Backends/sources/protection/FALCO_upstream/datasets/features/celebahq/arcface_features.pt`
- `Backends/sources/protection/FALCO_upstream/datasets/features/celebahq/image_filenames.txt`
- `Backends/sources/protection/FALCO_upstream/datasets/fake/fake_dataset_stylegan2_ffhq512-0.7-1-ArcFace/arcface_features.pt`
- `Backends/sources/protection/FALCO_upstream/datasets/fake/fake_dataset_stylegan2_ffhq512-0.7-1-ArcFace/arcface_auto_euclidean_nn_map_celebahq.json`
- `Backends/sources/protection/FALCO_upstream/datasets/inv/celebahq/latent_codes/0.pt`
- `Backends/sources/protection/FALCO_upstream/datasets/inv/celebahq/latent_codes/1.pt`
- `Backends/sources/protection/FALCO_upstream/datasets/inv/celebahq/data/0_recon.jpg`
- `Backends/sources/protection/FALCO_upstream/datasets/inv/celebahq/data/1_recon.jpg`
- `Backends/sources/protection/FALCO_upstream/datasets/anon/celebahq/celebahq-train+val+test_anon_W+_m-0.0_lambda-id-10.0_lambda-attr-0.1_adam-0.01_epochs-1_arcface_auto_euclidean_nn_map_celebahq/data/0.jpg`
- `Backends/sources/protection/FALCO_upstream/datasets/anon/celebahq/celebahq-train+val+test_anon_W+_m-0.0_lambda-id-10.0_lambda-attr-0.1_adam-0.01_epochs-1_arcface_auto_euclidean_nn_map_celebahq/data/1.jpg`
- `tmp/falco_extract_mps.log`
- `tmp/falco_extract_mps_summary.json`
- `tmp/falco_create_fake_mps.log`
- `tmp/falco_pair_nn.log`
- `tmp/falco_invert_mps.log`
- `tmp/falco_anonymize_mps.log`
- `tmp/falco_pipeline_mps_summary.json`
- Fidelity note:
- this is a reduced end-to-end smoke pipeline, not a full upstream-scale benchmark path
- the verified route uses a tiny local CelebA-HQ-style fixture, ArcFace-only features, `stylegan2_ffhq512` fake-dataset generation, `--skip-align` inversion, and one anonymization epoch
- the original upstream detector/alignment stack, the FaRL/CLIP/DINO feature branches, and benchmark-scale anonymization settings are still not claimed verified here

Protection backend: deepprivacy2
- Status: verified
- Upstream repo: preserved backend is `Backends/sources/protection/DeepPrivacy2_upstream` from `hukkelas/deep_privacy2`
- Upstream snapshot: imported 2026-03-30 at commit `f4d8f09d1eb8f758c89cf1795ae41f20533942d3`
- Defense layer: proactive anonymization
- Assets used:
- source snapshot: `Backends/sources/protection/DeepPrivacy2_upstream`
- asset root: `Backends/assets/protection/deepprivacy2`
- workdir root: `Backends/workdirs/protection/deepprivacy2`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/deepprivacy2/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- `scripts/import_protection_assets.py`
- `Backends/sources/protection/DeepPrivacy2_upstream/anonymize.py`
- `Backends/sources/protection/DeepPrivacy2_upstream/dp2/utils/__init__.py`
- `Backends/sources/protection/DeepPrivacy2_upstream/dp2/utils/vis_utils.py`
- `Backends/sources/protection/DeepPrivacy2_upstream/dp2/detection/__init__.py`
- Local adaptations:
- imported the official upstream source into the protection subtree and removed nested Git metadata after vendoring
- replaced the active legacy `DeepPrivacy` backend entry with `DeepPrivacy2`
- added a repo-local wrapper at `Models/protection/deepprivacy2/wrapper.py`
- rebuilt the local env with Homebrew Python 3.11 because the earlier Python 3.14 bootstrap path failed on practical inference dependencies
- narrowed the DeepPrivacy2 bootstrap package set toward face-anonymization inference instead of the full training-time dependency stack
- patched git-based pip installs to use `--no-build-isolation`, which was required for `face_detection` to see the already-installed local `torch`
- made DensePose imports optional for face-only execution in `dp2/utils/__init__.py`, `dp2/utils/vis_utils.py`, and `dp2/detection/__init__.py`
- made `anonymize.py` lazily import `moviepy` and fall back to Pillow EXIF handling so image anonymization no longer hard-depends on video and detectron2 utilities at import time
- fixed `dp2.utils.load_config()` so absolute config paths now resolve output directories under the repo-owned DeepPrivacy2 workdir instead of producing doubled absolute-path fragments
- forced the wrapper's offline run path through repo-owned assets and workdirs, including `WANDB_MODE=offline`
- seeded the DSFD detector cache with the locally imported Hugging Face mirror checkpoint under the exact filename expected by the installed `face_detection` package, avoiding the broken `loke` download URL during inference
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- MPS / Apple Metal: reduced-resolution one-image wrapper smoke test verified outside sandbox
- MPS smoke-test command:
- `MPLCONFIGDIR=Backends/workdirs/protection/deepprivacy2/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=1 Backends/workdirs/protection/deepprivacy2/venv/bin/python Models/protection/deepprivacy2/wrapper.py tmp/deepprivacy2_mps_in tmp/deepprivacy2_mps_out_small3 --python-bin Backends/workdirs/protection/deepprivacy2/venv/bin/python --device auto --max-res 256`
- MPS smoke-test output:
- `tmp/deepprivacy2_mps_out_small3/erling.jpg`
- MPS smoke-test summary:
- `tmp/deepprivacy2_mps_out_small3/_deepprivacy2_summary.json`
- Fidelity note:
- wrapper now succeeds on a reduced-resolution one-image MPS smoke test through the upstream `configs/anonymizers/face.py` face anonymizer entrypoint
- the active checkpoint import now uses the author's Hugging Face Space mirror because the original host was unavailable during local staging
- the verified local path still includes compatibility patches that make DensePose- and detectron2-dependent full-body helpers optional, so this should be described as a verified face-anonymization path rather than a full untouched upstream environment

Protection backend: facial_attributes_obfuscation
- Status: verified
- Upstream repo: preserved backend is `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream` from `RuoyuChen10/Facial_Attributes_Obfuscation`
- Upstream snapshot: imported 2026-03-30 at commit `2443c8b285f0092990366a8306753d66b53d3fb4`
- Defense layer: proactive anonymization / attribute obfuscation
- Assets used:
- source snapshot: `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream`
- asset root: `Backends/assets/protection/facial_attributes_obfuscation`
- workdir root: `Backends/workdirs/protection/facial_attributes_obfuscation`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/facial_attributes_obfuscation/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/models/resnet.py`
- `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/Generator/main.py`
- `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/Generator/solver.py`
- `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/Generator/vgg_model.py`
- `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/Generator/metrics/fid.py`
- `Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/Generator/metrics/lpips.py`
- Local adaptations:
- imported the upstream source into the protection subtree and removed nested Git metadata after vendoring
- added a repo-local stage wrapper at `Models/protection/facial_attributes_obfuscation/wrapper.py`
- rebuilt the local env with Homebrew Python 3.11 because this torch-heavy stack should not stay on the earlier Python 3.14 path
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- added `munch` to the env bootstrap because the vendored StarGAN metric path imports it through `Generator/core/data_loader.py`
- fixed the stage-1 wrapper defaults so the vendored sample-image path is real instead of pointing at the dead `./align_arc` placeholder
- added `pytorch-lightning` to the env bootstrap because the vendored FaceParser checkpoint is a Lightning pickle
- added a legacy `models.resnet` compatibility shim so the vendored attribute checkpoint can unpickle on the preserved repo layout
- verified the stage-1 scoring path on native MPS outside sandbox with fallback disabled
- stage 1 is native MPS end to end
- this does not mean the full multi-stage anonymization pipeline is native MPS end to end
- patched the stage-2 wrapper and generator entrypoint so runtime device selection now follows `cuda -> mps -> cpu` instead of forcing CUDA-oriented defaults
- generator train/test now receive the repo-managed SENet and LPIPS weight paths from the wrapper instead of relying on dead relative placeholders
- patched the generator metric helpers so LPIPS no longer hardcodes `.cuda()` tensors and FID can run on the resolved runtime device
- patched the generator SENet loader and checkpoint restore path so checkpoint loads respect the requested runtime device
- patched the generator SENet weight load for modern PyTorch so the vendored pickle-style checkpoint restores with `weights_only=False`
- added a second SENet load fallback for the released `resnet50_scratch_weight.pkl`, which is actually a plain pickle dict of NumPy arrays rather than a native Torch checkpoint
- improved the SENet loader so the released `resnet50_scratch_weight.pkl` is now translated from ResNet-style key names into the vendored SENet layout and partially loaded instead of immediately failing into full random init
- the translated preload currently matches 187 tensors and leaves the remaining SENet-specific layers at initialization, which is still not benchmark-faithful but is materially better than the earlier full random-init smoke path
- added an explicit `--allow-random-init` path as a last-resort fallback, but the preferred local Stage 2 smoke route no longer needs it
- improved the repo wrapper so generator path-style flags are resolved to absolute paths before launching `Generator/main.py`, avoiding cwd-fragile smoke commands
- verified Stage 2 generator train and test on native MPS outside sandbox through the repo wrapper using a tiny local CelebA-style smoke fixture
- this now proves the vendored later-stage StarGAN path can run on MPS for both backward and inference execution
- verified Stage 2 MPS smoke commands:
- train:
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=Backends/workdirs/protection/facial_attributes_obfuscation/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python Models/protection/facial_attributes_obfuscation/wrapper.py --python-bin Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python --device mps --allow-random-init generator-train --celeba_image_dir tmp/fao_stage2_smoke/images --attr_path tmp/fao_stage2_smoke/list_attr_celeba.txt --num_iters 1 --num_iters_decay 1 --batch_size 1 --sample_step 10000 --model_save_step 1 --lr_update_step 10000 --log_step 1 --use_tensorboard false --log_dir tmp/fao_stage2_smoke/out2/logs --model_save_dir tmp/fao_stage2_smoke/out2/models --sample_dir tmp/fao_stage2_smoke/out2/samples --result_dir tmp/fao_stage2_smoke/out2/results`
- test:
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=Backends/workdirs/protection/facial_attributes_obfuscation/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python Models/protection/facial_attributes_obfuscation/wrapper.py --python-bin Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python --device mps --allow-random-init generator-test --celeba_image_dir tmp/fao_stage2_test/images --attr_path tmp/fao_stage2_test/list_attr_celeba.txt --test_iters 1 --batch_size 1 --use_tensorboard false --log_dir tmp/fao_stage2_test/out/logs --model_save_dir tmp/fao_stage2_smoke/out2/models --sample_dir tmp/fao_stage2_test/out/samples --result_dir tmp/fao_stage2_test/out/results`
- verified Stage 2 MPS smoke artifacts:
- train log:
- `tmp/fao_stage2_mps_train_save.log`
- saved checkpoints:
- `tmp/fao_stage2_smoke/out2/models/1-G.ckpt`
- `tmp/fao_stage2_smoke/out2/models/1-D.ckpt`
- test log:
- `tmp/fao_stage2_mps_test.log`
- generated results:
- `tmp/fao_stage2_test/out/results/result.txt`
- `tmp/fao_stage2_test/out/results/real_ima/1-images.jpg`
- `tmp/fao_stage2_test/out/results/real_ima/2-images.jpg`
- `tmp/fao_stage2_test/out/results/fake_ima/1_1-images.jpg`
- `tmp/fao_stage2_test/out/results/fake_ima/2_30-images.jpg`
- verified stricter CPU Stage 2 smoke path without `--allow-random-init` using translated SENet preload:
- train:
- `Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python Models/protection/facial_attributes_obfuscation/wrapper.py --python-bin Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python --device cpu generator-train --celeba_image_dir tmp/fao_stage2_smoke/images --attr_path tmp/fao_stage2_smoke/list_attr_celeba.txt --num_iters 1 --num_iters_decay 1 --batch_size 1 --sample_step 10000 --model_save_step 1 --lr_update_step 10000 --log_step 1 --use_tensorboard false --log_dir tmp/fao_stage2_smoke_no_random/logs --model_save_dir tmp/fao_stage2_smoke_no_random/models --sample_dir tmp/fao_stage2_smoke_no_random/samples --result_dir tmp/fao_stage2_smoke_no_random/results`
- test:
- `Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python Models/protection/facial_attributes_obfuscation/wrapper.py --python-bin Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python --device cpu generator-test --celeba_image_dir tmp/fao_stage2_test/images --attr_path tmp/fao_stage2_test/list_attr_celeba.txt --test_iters 1 --batch_size 1 --use_tensorboard false --log_dir tmp/fao_stage2_test_no_random/logs --model_save_dir tmp/fao_stage2_smoke_no_random/models --sample_dir tmp/fao_stage2_test_no_random/samples --result_dir tmp/fao_stage2_test_no_random/results`
- verified stricter CPU Stage 2 artifacts:
- `tmp/fao_stage2_smoke_no_random/models/1-G.ckpt`
- `tmp/fao_stage2_smoke_no_random/models/1-D.ckpt`
- `tmp/fao_stage2_test_no_random/results/real_ima/1-images.jpg`
- `tmp/fao_stage2_test_no_random/results/fake_ima/1_30-images.jpg`
- verified command:
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=/Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/facial_attributes_obfuscation/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 /Users/jeromeharianto/Documents/School2/FYP/Backends/workdirs/protection/facial_attributes_obfuscation/venv/bin/python -u -m utils.score --mode arcface --dataset-root /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/images --data-list /Users/jeromeharianto/Documents/School2/FYP/Backends/sources/protection/Facial_Attributes_Obfuscation_upstream/images/label.txt --num-classes 8631 --pre-trained /Users/jeromeharianto/Documents/School2/FYP/Backends/assets/protection/facial_attributes_obfuscation/pretrained/ArcFace-8631.pth --seg-pre-trained /Users/jeromeharianto/Documents/School2/FYP/Backends/assets/protection/facial_attributes_obfuscation/pretrained/FaceParser.ckpt --attr-pre-trained /Users/jeromeharianto/Documents/School2/FYP/Backends/assets/protection/facial_attributes_obfuscation/pretrained/Face-Attributes2.pth --cls-device mps --seg-device mps --attr-device mps --save-dir /Users/jeromeharianto/Documents/School2/FYP/tmp/fao_arc_mode`
- verified artifacts:
- `tmp/fao_arc_mode/Record.json`
- `tmp/fao_arc_mode/Img/0001_01-0.jpg/0001_01-0-gradcam.jpg`
- `tmp/fao_arc_mode/Img/0001_01-0.jpg/0001_01-0-part-score.jpg`
- `tmp/fao_arc_mode/Img/0002_01-6.jpg/0002_01-6-gradcam.jpg`
- `tmp/fao_arc_mode/Img/0002_01-6.jpg/0002_01-6-part-score.jpg`
- `tmp/fao_stage1_mps.log`
- `tmp/fao_stage1_mps_summary.json`
- Fidelity note:
- Stage 1 plus Stage 2 train/test now have native MPS smoke verification through the repo wrapper path
- the original benchmark-faithful SENet checkpoint situation is still unresolved because the preserved `resnet50_scratch_weight.pkl` asset is a ResNet-style release rather than a clean native dump of the vendored SENet module
- the improved Stage 2 path now uses translated partial pretrained initialization instead of full random init, so it is stronger than the earlier smoke proof but still approximation-grade rather than benchmark-faithful

Protection backend: unidefense
- Status: verified
- Upstream repo: preserved backend is `Backends/sources/protection/UniDefense_upstream` from `VISION-SJTU/UniDefense`
- Upstream snapshot: imported 2026-03-30 at commit `6213c4e7cf932c2bedd27711bac0df6a0fa08afb`
- Defense layer: post-hoc detection
- Assets used:
- source snapshot: `Backends/sources/protection/UniDefense_upstream`
- asset root: `Backends/assets/protection/unidefense`
- workdir root: `Backends/workdirs/protection/unidefense`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/unidefense/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- `Backends/sources/protection/UniDefense_upstream/engine/abstract_engine.py`
- `Backends/sources/protection/UniDefense_upstream/engine/forgery_engine.py`
- `Backends/sources/protection/UniDefense_upstream/model/efficientnet/exp.py`
- Local adaptations:
- imported the official upstream source into the protection subtree and removed nested Git metadata after vendoring
- added a repo-local wrapper at `Models/protection/unidefense/wrapper.py`
- wrapper preserves the original config-driven test entrypoint instead of inventing a fake single-image interface
- rebuilt the local env with Homebrew Python 3.11 because this torch-heavy stack is a better fit than the earlier Python 3.14 path
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- patched runtime device resolution so the engine can run on `cuda`, `mps`, or `cpu` instead of assuming CUDA
- patched test-time startup so a smoke run can proceed with `--allow-random-init` when a benchmark checkpoint is not staged
- patched the custom EfficientNet spatial-frequency block so the MPS smoke path now completes natively without the earlier CPU detour
- built a tiny local FF++-style smoke fixture under `tmp/unidefense_smoke_ffpp` and matching configs under `tmp/unidefense_smoke_configs`
- verified a native MPS end-to-end smoke run outside sandbox with fallback disabled
- verified command:
- `MPLCONFIGDIR=Backends/workdirs/protection/unidefense/matplotlib PYTORCH_ENABLE_MPS_FALLBACK=0 Backends/workdirs/protection/unidefense/venv/bin/python Models/protection/unidefense/wrapper.py --config tmp/unidefense_smoke_configs/model_udeb4_smoke.yml --ds-config tmp/unidefense_smoke_configs/data_ffpp_smoke.yml --engine FE --device mps --python-bin Backends/workdirs/protection/unidefense/venv/bin/python --allow-random-init`
- verified artifacts:
- `tmp/unidefense_smoke_summary.json`
- `Backends/sources/protection/UniDefense_upstream/runs/UDEB4/smoke_ffpp_mps/test.txt`
- Fidelity note:
- this is a real native MPS end-to-end wrapper smoke test, but it is still only a smoke test
- the verified path uses a two-image local FF++-style fixture plus `--allow-random-init`, so it proves runtime execution and device viability, not benchmark-quality detector performance

Protection backend: m2f2_det
- Status: verified
- Upstream repo: preserved backend is `Backends/sources/protection/M2F2_Det_upstream` from `CHELSEA234/M2F2_Det`
- Upstream snapshot: imported 2026-03-30 at commit `c0b7f8fb6b5307e28950c0914ac4ae95f530def1`
- Defense layer: post-hoc interpretable detection
- Assets used:
- source snapshot: `Backends/sources/protection/M2F2_Det_upstream`
- asset root: `Backends/assets/protection/m2f2_det`
- workdir root: `Backends/workdirs/protection/m2f2_det`
- Local files patched:
- `Backends/manifest.json`
- `scripts/bootstrap_backends.py`
- `Models/protection/m2f2_det/wrapper.py`
- `Models/protection/shared.py`
- `scripts/bootstrap_protection_envs.py`
- `Backends/sources/protection/M2F2_Det_upstream/stage_1_detection_inference.py`
- `Backends/sources/protection/M2F2_Det_upstream/dataset/vdataset.py`
- `Backends/sources/protection/M2F2_Det_upstream/sequence/runjobs_utils.py`
- `Backends/sources/protection/M2F2_Det_upstream/sequence/models/M2F2_Det/models/model.py`
- `scripts/create_m2f2_smoke_fixture.py`
- Local adaptations:
- imported the official upstream source into the protection subtree and removed nested Git metadata after vendoring
- added a repo-local stage wrapper at `Models/protection/m2f2_det/wrapper.py`
- standardized future env bootstrap through `scripts/bootstrap_protection_envs.py`
- rebuilt the `m2f2_det` venv on Homebrew Python 3.11 after confirming the older Python 3.14 env was the oddball local runtime that kept failing MPS checks inside this backend workflow
- patched stage-1 inference to follow `cuda -> mps -> cpu`, remove hard `.cuda()` calls, skip `DataParallel` off-CUDA, and load repo-owned checkpoints via `FYP_M2F2_ASSET_DIR`
- patched the restore path to opt out of PyTorch 2.6+ `weights_only=True` when loading optimizer checkpoints
- extended the bootstrap/import path so the M2F2 venv installs `munch` and the asset importer pulls a repo-local snapshot of `openai/clip-vit-large-patch14-336`
- stage-1 inference now prefers that repo-local CLIP snapshot through `FYP_M2F2_CLIP_DIR` instead of ambient Hugging Face cache state
- added smoke-only wrapper overrides for `--data-root`, `--test-split`, `--batch-size`, and `--num-frames`
- added `scripts/create_m2f2_smoke_fixture.py` so local smoke data uses distinct FF++-style frames instead of repeating the same image many times
- staged the official torchvision EfficientNet-B4 weights into `Backends/assets/protection/m2f2_det/torch_home/hub/checkpoints/efficientnet_b4_rwightman-23ab8bcd.pth`
- confirmed the released stage-1 checkpoint now gets past backbone initialization in strict mode
- traced the remaining load failure to an upstream checkpoint/model-registration quirk rather than a missing asset: the released checkpoint contains `linear_1`, `linear_2`, `linear_3`, `clip_reduction`, and `bridge_adapter_proj`, but not registered `bridge_adapter.*` tensors
- reverted the stage-1 model registration pattern so `bridge_adapter` is kept as a plain Python list and `linear_lst` is kept as a plain list over the separately registered `linear_1/2/3` layers; this matches the released checkpoint layout closely enough for strict restore
- reran strict stage-1 inference on a distinct tiny FF++-style smoke fixture and the full validation loop completed on CPU without random-init fallback
- confirmed that `torch.backends.mps.is_available()` is false inside the sandbox for this env but true outside it on the host machine
- reran the same strict stage-1 smoke path outside sandbox on `mps`, and the full 27-batch validation loop completed successfully
- verified command:
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=Backends/workdirs/protection/m2f2_det/matplotlib Backends/workdirs/protection/m2f2_det/venv/bin/python Models/protection/m2f2_det/wrapper.py --python-bin Backends/workdirs/protection/m2f2_det/venv/bin/python --device cpu --batch-size 8 --num-frames 21 --data-root tmp/m2f2_smoke_ffpp_distinct --test-split tmp/m2f2_smoke_ffpp_distinct/test.json stage1-det`
- `PYTHONUNBUFFERED=1 PYTHONFAULTHANDLER=1 MPLCONFIGDIR=Backends/workdirs/protection/m2f2_det/matplotlib Backends/workdirs/protection/m2f2_det/venv/bin/python Models/protection/m2f2_det/wrapper.py --python-bin Backends/workdirs/protection/m2f2_det/venv/bin/python --device mps --batch-size 8 --num-frames 21 --data-root tmp/m2f2_smoke_ffpp_distinct --test-split tmp/m2f2_smoke_ffpp_distinct/test.json stage1-det`
- verified artifacts:
- future stage-1 outputs now route under `Backends/workdirs/protection/m2f2_det/stage1-det/` instead of polluting the vendored source tree
- logged metrics:
- `Test AUC-ROC: 0.5000`
- `Test accuracy: 0.8889`
- Fidelity note:
- stage-1 detector inference is now strictly runnable locally on CPU and on MPS with the released checkpoint and staged official assets
- the key caveat is that checkpoint compatibility depends on preserving the released upstream registration quirk where the bridge transformer blocks are not part of the serialized state dict
- this is much closer to checkpoint-faithful reproduction than the earlier random-init fallback path, but the verified result here is still only a small-fixture smoke verification rather than a benchmark-scale detector claim
- the current MPS verdict should be described as verified outside sandbox; the sandbox itself can still report false-negative MPS availability even when the host run succeeds
