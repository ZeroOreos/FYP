FYP constructor

Goal
- Build an adversarial recognizer-training system for face recognition, not a generic manipulated-face generator ensemble.
- Build this repo as the full and final model-training repo, not as a temporary prototype repo.
- Treat the project as a two-level ensemble: recognizer ensemble plus attacker ensemble.
- Use the strongest research-backed recipe that is still practical in this repo without overstating what the literature has already standardized.
- Test one blunt question: does attack-pool diversity beat strongest-single-attack training.
- Test one second question: how should recognizer ensembles be used productively instead of cosmetically.
- Target claim strength: a moderately strong multi-recognizer, multi-attack ensemble should improve robustness and transfer materially over cleaner or narrower baselines, while paying a visible but controlled clean-performance cost.

Paper position
- This is a recognizer-hardening project.
- This is an ensemble-aware recognizer-training and evaluation project.
- It is not a detector paper.
- It is not a backend collection paper.
- It is not a generic adversarial-training paper with face data swapped in.
- Do not claim a mature face-recognition standard for full joint attack-plus-recognizer co-training; that exact recipe is still underexplored.
- Files under `notes/reports/` are downstream report documents only.
- Reports base themselves on the repo; the repo must not be reinterpreted to match a stale report draft.
- When report wording conflicts with code, configs, run outputs, or these core notes, update the report and keep the repo-side source of truth unchanged.

Current paper-strength interpretation
- The defended main condition should now be described as `full-ensemble`.
- `full-ensemble` is the condition intended to support the paper’s architectural claim.
- That claim is specifically:
- one explicit target recognizer
- one explicit external recognizer ensemble
- one explicit attacker ensemble
- joint pressure from both ensembles onto the target recognizer
- Do not collapse this back into the older `joint_pool` wording.
- `joint_pool` remains a legacy composite-target path and should not be treated as the canonical paper interpretation.
- The current paper-ready target backbone is `iResNet100` for both the serious test path and the main run.
- The current trusted verification-stage contract is the successful `full-ensemble-test` setup:
- warmup `128`
- shallow `48`
- full `32`
- attack chunk `32`
- `2/1/1` stage schedule
- recognizer ensemble enabled
- attacker ensemble enabled
- Any later config drift should be reset back to that successful test contract before changing only the dimensions needed for the longer paper run.
- The current main full-paper condition extends that same contract rather than changing the basic method:
- warmup `128`
- shallow `48`
- full `32`
- same recognizer ensemble
- same attacker ensemble
- longer `24` epoch schedule with `3/9/12` stage split
- The method claim should be treated as architecture-strong but result-dependent:
- architecture claim can be made from the implementation
- effectiveness claim must come from the finished ladder results
- Verification is part of the intended paper metric surface, but it must be implemented efficiently enough not to distort the main training loop.
- Robustness evaluation is now separated from training attackers through `eval_attackers`, so clean-only and recognizer-only rows can be trained without adversarial pressure but still receive a final fair `PGD/BPFA/DFANet` robust evaluation.
- The current verification path should be described as:
- deduplicate unique pair-image references first
- batch image loading through a dataloader
- prefer extracted image-file references instead of random-access shard-member references when available
- resize verification images on the CPU side before transfer
- batch embedding extraction on GPU
- vectorize cosine scoring in chunks
- compute verification metrics exactly from sorted scores and cumulative TP/FP counts rather than threshold-grid approximations
- report progress for embedding extraction, DDP gather, scoring, and metric computation during verification probes
- keep the verification metrics unchanged while moving more of the work to the GPU side
- This is a performance implementation detail, not a metric-definition change.
- Use `scripts/probe_verification.py` when verification appears to stall; it isolates pair-bundle IO, embedding extraction, DDP sharding, and scoring throughput from the rest of training.

Research anchors
- `ArcFace` is still the main baseline margin-loss line.
- `CosFace` is a foundational additive-margin baseline that makes the paper comparisons easy to justify.
- `CurricularFace` is a logical third recognizer because it extends the same margin-loss family with curriculum-style hardness control.
- `PGD` is the logical white-box optimization anchor.
- `BPFA` is the logical modern transfer-attack anchor.
- `DFANet` is the logical FR-specific feature-level attack anchor.
- Madry-style robust optimization is the right anchor for the defended recognizer update rule.
- Tramer-style ensemble adversarial training is the right anchor for decoupled external perturbation sources and rotating diversity pressure.
- DFANet-style face-recognition attacks support feature-level transfer pressure as more useful than label-only pressure in this domain.
- `AdvFaceGAN`, `Adv-Makeup`, and `Greedy-DiM` give the surrogate-attack side distinct face-specific manipulation mechanisms.
- `Partial FC` and sub-center variants are relevant because this project wants large-class training and noisy identities.
- `WebFace4M` is the best local serious training set under a tight storage budget.
- `WF42M` remains the ceiling target, not the default local working set.
- Recent ensemble-attack theory supports two usable rules: transferability improves with surrogate vulnerability and surrogate diversity.
- Recent FR attack work keeps moving toward harder surrogate diversity, stronger black-box transfer, and more system-level realism.
- Recent FR robustness work says adversarial robustness alone is not enough; OOD corruptions and appearance variations must also be evaluated.

Main comparison
- clean baseline
- strongest single attack
- primary attacker set
- primary-plus-surrogate attacker set
- broader robustness evaluation

Core hypothesis
- Diversity matters more than raw attack strength alone.
- Recognizer diversity and attack diversity should improve robustness transfer across unseen recognizers and unseen attacks.
- Surrogate diversity on the attack side matters at least as much as perturbation diversity.
- Any robustness gain must be reported next to clean-performance loss.
- Expected outcome shape: the biggest separations should appear in robust accuracy, robust loss, attack success rate, and transfer behavior, not necessarily in clean accuracy alone.
- Expected ranking shape: clean-only baseline should be strongest on clean metrics, while the broader multi-attack and multi-recognizer conditions should be strongest on broad robustness if the claim is correct.

Best training approach for this repo
- Train and evaluate the system as ensemble-aware recognizer hardening.
- Build directly toward the final trainable system, then scale backbone and dataset once the contract is verified.
- Keep the defended side heterogeneous and small, not wide and redundant.
- The current engineering path includes both a legacy `joint_pool` composite target and a newer external recognizer-ensemble path.
- Do not collapse those two ideas in the paper wording.
- `joint_pool` means one composite target with internal recognizer heads.
- the paper goal means one target model trained under external recognizer-ensemble pressure plus attacker-ensemble pressure.
- Keep the attacker side diverse and explicit in both paths.
- Use the recognizer ensemble and attacker ensemble for attack generation, hard-pair mining, sampling pressure, and transfer evaluation.
- Keep the target loss simple and strong: margin-based identity classification plus clean/adversarial consistency and targeted hard-sample emphasis.
- The claim direction should now prefer explicit target-plus-ensembles wording over the older joint-pool wording.
- The paper wording should present the full condition as a moderately strong ensemble recipe, not as a claim of a finalized or dominant FR robustness standard.
- Under a local `20GB` class budget, do not start with full adversarial pressure.
- Use a `3`-stage depth curriculum: clean warmup, shallow adversarial depth, then full adversarial depth.
- Stage `1` should be clean-only so the backbone and embedding geometry stabilize first.
- Stage `2` should use weak fast attacks with reduced budgets and partial attacker rotation.
- Stage `3` should use the full primary-attacker rotation and the final attack budgets.
- Heavy semantic attacks should stay periodic or eval-time and appear only late if used at all.
- Full robust evaluation should be periodic and mostly posthoc, not always-on inside every epoch.
- Verification can be more frequent than full robust evaluation, but if it is kept in-loop it should not overlap with the heaviest robust-eval epochs and should use the batched path above.
- `ArcFace` is the main tuned recipe.
- `CosFace` and `CurricularFace` are part of the defended recognizer ensemble, not side baselines.
- Completed ladder evidence supports the intended shape of the claim: `full-ensemble` trades clean validation and runtime for stronger final robust validation than `attack-ensemble-only`.
- Final clean validation across the pulled ladder: `baseline-clean` `99.23%`, `recognizer-ensemble-only` `99.23%`, `attack-ensemble-only` `93.98%`, `full-ensemble` `93.40%`, and `single-attack-only` `79.69%`.
- Final robust validation across the attacked rows: `single-attack-only` `30.00%` under its trained `PGD` eval, `attack-ensemble-only` `52.32%` averaged over `PGD/BPFA/DFANet`, and `full-ensemble` `56.55%` averaged over `PGD/BPFA/DFANet`.
- Comparability caveat: rerun `single-attack-only` posthoc robust with the shared `PGD/BPFA/DFANet` eval suite before using it in an averaged robustness table.
- Final `full-ensemble` versus `attack-ensemble-only`: robust accuracy `56.55%` versus `52.32%`, average attack success `39.45%` versus `44.32%`.
- The robustness gain appears across all trained in-loop attack families in the final all-attack eval: `PGD` `52.55%` versus `45.45%`, `BPFA` `69.56%` versus `66.09%`, and `DFANet` `47.55%` versus `45.43%`.
- Final posthoc verification is strongest for clean and recognizer-only rows, while attacked rows pay a visible verification cost.
- `attack-ensemble-only` final posthoc verification is still strong: pair accuracy `98.33%`, `EER=1.78%`, `ROC-AUC=99.75%`.
- `single-attack-only` final posthoc verification is weak: pair accuracy `89.74%`, `EER=12.82%`, `ROC-AUC=92.01%`.
- Final `full-ensemble` posthoc verification is now pulled locally: pair accuracy `98.21%`, `EER=1.93%`, `ROC-AUC=99.73%`.
- Local artifact caveat: the currently pulled `baseline-clean/posthoc-robust.json` and `recognizer-ensemble-only/posthoc-robust.json` are skipped artifacts with `reason=no_attackers`; rerun those posthoc robust probes after syncing the `eval_attackers` patch before comparing clean-only or recognizer-only robustness against attacked rows.
- Local artifact caveat: `single-attack-only` also needs a shared-suite posthoc robust probe because the fair paper comparison should evaluate every row against the same `PGD/BPFA/DFANet` suite.
- Crosscheck caveat: older `summary.json.best` fields used max-is-best for every metric, so `best.val_verification_eer` and `best.val_robust_attack_success_rate` are not paper-safe in existing artifacts; use final/latest/posthoc values instead.
- Paper should argue robustness benefit rather than clean or verification dominance from recognizer ensembling.
- Posthoc verification should be labeled as embedding verification when classifier/margin weights are skipped because the current manifest class count differs from the checkpoint class count.
- The paper should explicitly list modern attacker stress testing as the next step rather than implying it is already covered by the current in-loop attack families.
- The current ladder supports the framework and ablation claim; modern transfer, diffusion/makeup, morphing, and physical-style attackers require faithful separate implementations or cached generated adversarial images before they can be counted as evidence.
- The recommended future stress lineup is higher-budget `PGD`/CW/APGD-style checks, TransMix-style transfer attacks, diffusion or adversarial-makeup attacks, Greedy-DiM-style morphing evaluation, and FR-plus-FAS/PAD realism tests.
- Do not relabel existing `PGD`, `BPFA`, or `DFANet` outputs as modern stress attacks; the paper should keep them as the trained/in-loop attacker family and reserve modern attackers for follow-up validation.

Primary target
- Base defended recognizer set: `ArcFace` + `CosFace` + `CurricularFace`.
- `ArcFace` is the canonical anchor.
- `ArcFace` should be treated as the main serious recipe on `iResNet100`.
- `CosFace` and `CurricularFace` provide the foundational same-family comparisons for the paper.
- Final target is a full recognizer trained in this repo, with `ArcFace` fixed to `iResNet100`.
- Practical early runs should use the smaller backbone as a real verification stage of the final training contract, not as a throwaway placeholder path.
- Current repo entry point for the pooled path: `target_model = joint_pool`.

Data ladder
- local verification data: `WebFace4M` subset manifests on the small backbone
- local serious data: aligned `WebFace4M`
- ceiling data: aligned `WF42M`
- avoid inventing a random 20GB `WF42M` slice unless it is identity-balanced and alignment-matched
- current local serious target: keep `WebFace4M` as the storage budget dataset and finish its trainable ingestion path cleanly
- `CelebA` is retired from the active training and testing plan

Ceiling contract
- RetinaFace-class 5-point alignment
- `112x112` crop
- `WebFace42M`
- `IR-ResNet200`
- `512-D` embedding
- `ArcFace` with `s=64`, `m=0.5`
- `Partial FC` at `0.3`
- distributed PyTorch
- AMP
- gradient checkpointing

Recognizer plan
- implement `ArcFace` first
- keep `ArcFace` as the main tuned recognizer and move it to `iResNet100`
- keep `CosFace` and `CurricularFace` in the same training contract as the two other ensemble recognizers
- use held-out recognizers for transfer evaluation instead of expanding the trained ensemble indefinitely
- pull native upstream code into `Backends` when the native implementation adds real value or compatibility
- keep one training API in `Training/` even if native backends are pulled for reference or asset reuse

Attack plan
- keep a compact primary attacker set and a distinct surrogate attacker set for the core paper system
- primary attacker set: `PGD` + `BPFA` + `DFANet`
- primary set logic: cover white-box optimization, modern transfer attack pressure, and FR-specific feature-level attack pressure
- surrogate attacker set: `AdvFaceGAN` + `Adv-Makeup` + `Greedy-DiM`
- surrogate set logic: cover unrestricted dual-identity attack pressure, semantic makeup manipulation, and morphing pressure
- main-loop attackers should stay standard and cheap enough to rotate by batch
- morphing, makeup, and other generative attackers should be periodic-training or eval-time pressure, not the default every-batch path
- later realism extension: evaluate on FR plus FAS or PAD stacks, not only the matcher
- do not present the attack pool as a random collection of face manipulation tools; present it as a transfer-oriented attack ensemble

Ensemble logic
- ensemble training here means ensemble-informed training, not naive weight averaging
- use the recognizer ensemble as the frozen recognizer side of the training pressure
- use the attacker ensemble as the rotating attack side of the training pressure
- keep primary attackers and surrogate attackers as distinct roles, not one undifferentiated list
- rotate one primary attacker family per batch instead of blending all attacker families into every batch
- use a `50%` clean / `50%` adversarial batch policy as the final full-depth defended recognizer contract
- use a shallower adversarial fraction in the intermediate stage, not at full depth immediately
- split adversarial intent evenly between impersonation and dodging
- use the ensemble to mine hard pairs and hard negatives that survive across recognizers
- reserve held-out recognizers for transfer evaluation
- judge success by robustness transfer, not by ensemble complexity

Method rules
- freeze the split before serious runs
- minimize pure smoke-test framing; use early small-backbone runs to verify the real training contract
- log enough to reproduce each run
- do hypothesis-driven ablations only
- do not brute-force every combination
- prefer aligned and documented public training sets over ad hoc local mixtures
- if local storage is capped around `20GB`, prefer `WebFace4M` over a poorly justified `WF42M` slice
- keep a base set and an extended set; do not collapse everything into one monolithic experiment suite
- use broader surrogate diversity for attack generation than for the defended training target list
- keep early clean fraction high and early hard-pair weight low so the classifier learns identities before the attack pressure dominates
- treat a `WebFace4M` slice as medium-scale adversarial fine-tuning rather than pretending it is full-scale pretraining
- first serious slice schedule: `24` epochs with `2` epoch LR warmup and about `2-3` clean warmup epochs
- prefer effective batch around `512` if hardware allows; otherwise solve that later with accumulation rather than changing the core method
- treat data-path throughput as first-order: keep shard-local batching and avoid globally random access across compressed `WebFace4M` shards
- keep device tuning switchable between `MPS` and `CUDA`; do not collapse tuning around only one device path

Success condition
- The repo can run clean and attacked training end to end.
- The repo can run the base defended recognizer set through one consistent training and evaluation contract.
- The repo can run a real manifest-backed `WebFace4M` small-backbone training path with attacks, pairing, curriculum, checkpoints, and logs as the first stage toward the final model.
- The repo can scale from that verified small-backbone contract to the final chosen backbone without changing the fundamental training design.
- The paper can show whether rotating attack-pool diversity improves robustness on seen and unseen attacks without hiding the clean-cost tradeoff.
- The paper can show transfer robustness across unseen recognizers and report OOD corruption and variation robustness, not adversarial robustness alone.
- The paper can show that the multi-recognizer, multi-attack condition beats narrower ablations by a meaningful but not magical margin on robustness-oriented metrics.

References
- `[R1]` OODFace: Benchmarking Robustness of Face Recognition under Common Corruptions and Appearance Variations. Main support for broad robustness evaluation and the claim that adversarial robustness alone is too narrow. https://arxiv.org/abs/2412.02479
- `[R2]` Towards Deep Learning Models Resistant to Adversarial Attacks. Main support for Madry-style PGD adversarial training and robust optimization as the defended recognizer update rule. https://arxiv.org/abs/1706.06083
- `[R3]` Improving the Transferability of Adversarial Attacks on Face Recognition with Beneficial Perturbation Feature Augmentation. Main support for `BPFA` as a transferable FR base attack. https://arxiv.org/abs/2210.16117
- `[R4]` Ensemble Adversarial Training: Attacks and Defenses. Main support for decoupled external perturbation sources and rotating ensemble pressure instead of joint defended-pool optimization. https://arxiv.org/abs/1705.07204
- `[R5]` Towards Transferable Adversarial Attack against Deep Face Recognition. Main support for `DFANet`, feature-level transfer pressure, and the claim that feature-level attacks transfer better in face recognition. https://arxiv.org/abs/2004.05790
- `[R6]` AdvFaceGAN: a face dual-identity impersonation attack method based on generative adversarial networks. Main support for the unrestricted dual-identity surrogate-attacker slot. https://peerj.com/articles/cs-2904/
- `[R7]` Adv-Makeup: A New Imperceptible and Transferable Attack on Face Recognition. Main support for the semantic makeup surrogate-attacker slot. https://arxiv.org/abs/2105.03162
- `[R8]` Greedy-DiM: Greedy Algorithms for Unreasonably Effective Face Morphs. Main support for the morphing surrogate-attacker slot. https://arxiv.org/abs/2404.06025
- `[R9]` Adversarial Attacks on Both Face Recognition and Face Anti-spoofing Models. Main support for the FR plus FAS realism extension. https://www.ijcai.org/proceedings/2025/278
