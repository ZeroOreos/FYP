DiM upstream placeholder.

This folder reserves the backend source slot for any later import of an exact DiM upstream repository snapshot and its runnable assets.

Current local status:

- No preserved upstream repository has been imported here yet.
- The FYP currently uses `Modifiers/attack/dim/` as a local approximation path for smoke tests and pipeline integration.
- If an upstream DiM release is added later, record its provenance and exact verification notes in `notes/working/20_backend_adaptations.txt`.
