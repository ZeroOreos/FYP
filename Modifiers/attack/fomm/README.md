# FOMM Wrapper

This entrypoint is the FYP-facing wrapper for First Order Motion Model.

- Canonical pipeline entrypoint: `generate.py`
- Wrapper implementation: `wrapper.py`
- Expected backend source location: `Backends/sources/FOMM_upstream/`
- Expected backend assets location: `Backends/assets/fomm/`

Fidelity note:

This path is a best-effort wrapper around the original repository layout and pretrained assets. Results should be described as a best attempt at the original method unless the exact upstream commit, dependencies, config, and checkpoints are all pinned and verified.
