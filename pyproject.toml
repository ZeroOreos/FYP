[tool.ruff]
line-length = 100
target-version = "py39"
exclude = [
    "Dataset",
    "Results",
    "__pycache__",
]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "B",
    "UP",
]

[tool.ruff.lint.per-file-ignores]
"Models/**/testing/*.py" = ["E402"]
